// Shared between server-side messages (lib/chat/coins.ts) and client UI
// (app/chat/page.tsx's mailto/copy affordance) — deliberately has no
// 'server-only' import so both sides can use the same constant.
export const ADMIN_CONTACT_EMAIL = 'qurbanovzaur078@gmail.com';
